### Setup
```brew install buck```

### How to get up and running
- ```buck project demo_app``` (this will generate a '.xcworkspace' file)
- If you don't want to use Xcode: ```buck install -r demo_app```
- If you do want to use Xcode: ```open Apps/BuckDemoApp/BuckDemoApp.xcworkspace/```
